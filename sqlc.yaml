version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema"
    queries: "sql/queries"
    database:
      uri: postgresql://postgres:admin@localhost:5432/pincher
    gen:
      go:
        package: "database"
        out: "internal/database"
        sql_package: "database/sql"
